server:
  port: 9000

logging:
  level:
    org.springframework.security: info

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/sun_id
    username: root
    password: Root@123
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
      table-name: SPRING_SESSION